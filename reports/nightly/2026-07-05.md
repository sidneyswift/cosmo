# 🌙 Nightly Build — July 5, 2026

## Phase 1: Intelligence

**Where are we?** Recoup at ~$18K MRR. BYOA strategy is the core bet — customers bring their own AI tools and install Recoup plugins. The agent onboarding story was completed in early June (llms.txt for discovery, platform plugin for activation). But the marketing site — the public face — had no page showcasing the plugins. A potential customer visiting recoupable.com could find pricing, results, a calculator, an audit tool, blog posts, and developer docs — but couldn't see what plugins exist, what they do, or how to install them. For a company whose product IS plugins, that's a gap.

**Bottleneck:** Plugin discoverability on the marketing site. The plugins exist (29 skills across 4 plugins), the install infrastructure works, but the marketing funnel has no "here are our plugins" page. Prospects land on the site and see a platform pitch — not the concrete tools they'd install.

**What would surprise Sid?** The marketing site now has a /plugins page — a proper showcase of all 4 Recoup plugins with one-click install commands, capability lists, and audience targeting. It's the first nav item in the header.

**What I built tonight:** /plugins page on the marketing site (PR #39).

## Phase 2: Value Hierarchy

**Selected: Customer-impacting (Tier 1)**
- Directly helps every prospect and customer discover and install Recoup plugins
- Fills the marketing site gap: discovery → /plugins → install → use
- Aligned with BYOA strategy: plugins are the product, so they need a product page
- Category rotation: Jun 3 plugin/product, Jun 2 docs/DX, May 31 infrastructure → tonight is marketing/conversion (new category)

## Phase 3: What I Built

### `/plugins` page — Agent Plugin Marketplace Showcase

**Files:**
- `app/plugins/page.tsx` — client component with reveal-on-scroll animations
- `app/plugins/layout.tsx` — metadata + SEO
- `lib/copy/plugins.ts` — single source of truth for all copy (same pattern as /developers, /results)
- `lib/nav.ts` — added Plugins to header nav (first position) and footer

**Features:**
- Hero with "Agent Plugins for Music" headline + compatibility badges (Claude Code, Cowork, Codex, Cursor)
- 4 plugin cards in a 2-column grid:
  - **Research** (7 skills) — artist analytics, audience, competitive, playlists, trends, web intel, outreach
  - **Content** (3 skills) — content creation, social posts, short-form video
  - **Catalog Deals** (17 skills) — full deal pipeline from data room to IC memo
  - **Platform** (2 skills) — onboarding + health check
- Each card shows: description, skill count, audience, capabilities checklist, GitHub link, copyable terminal install command
- "How it works" — 3-step flow (Install → Connect → Go)
- CTA section linking to docs (API key) and pricing

**Technical:**
- Branch: `feat/plugins-page`
- PR: https://github.com/recoupable/marketing/pull/39
- Compiles clean (pre-existing TS error in demos page is unrelated)
- Design follows existing patterns (shadow-as-border, reveal animations, bitmap font headings)

## Phase 4: Verification

- ✅ Compilation passes ("Compiled successfully in 2.5s")
- ✅ TypeScript in new files is clean (pre-existing error is in demos page, not plugins)
- ✅ Branch pushed, PR created
- ✅ Nav links added to header and footer
- ⏳ Vercel preview will deploy automatically from PR

## Phase 5: What This Unlocks

- Prospects can now see exactly what Recoup offers as installable plugins
- Clear install path: see plugin → copy command → install → connected
- Sid can share recoupable.com/plugins in sales calls as a concrete "here's what you install"
- Foundation for adding more plugins as they ship (just add to `lib/copy/plugins.ts`)
