# === Databases (large, regenerable) ===
db/*.db
db/*.db-shm
db/*.db-wal
db/*.sqlite

# === Test/temp artifacts ===
test-thumbnails/
tmp/
slides/
*.tmp
*.lock

# === Runtime state (regenerated each boot) ===
openclaw-workspace-state.json

# === Gateway media staging (inbound Slack/upload files, transient) ===
media/

# === Stray auto-generated agent workspace (own .git, no commits — not repo content) ===
claude/

# === Sid's app repos live in ~/projects, never nested here ===
projects/

# === OS files ===
.DS_Store
Thumbs.db

# === Node ===
node_modules/

# === Secrets (never commit) ===
# Secrets live in 1Password, referenced via secrets.template
