# Memory — May 30, 2026

## Content Engine — Pipeline OOM (11:39 PM)
- Content queue fully depleted: 3 published, 52 rejected, 0 ready/pending
- `slack-review.js watch` triggers mini-pipeline to replenish
- `brain-first-generate` gets SIGKILL'd twice (cron + manual) during batch extraction phase
- Root cause: scanning 200 meeting files + LLM extraction spikes memory
- Both kills happen at the same stage: after extracting ~10 moments (2 batches of 5)
- Deferring to morning — the 7:45 AM morning-post cron can retry with more headroom
- **TODO:** Consider reducing batch size or meeting file count in brain-first-generate to avoid OOM

## Content Engine — Pipeline OOM, 3rd Kill (11:48 PM)
- `slack-review.js watch` triggered mini-pipeline again (queue empty, no ready drafts)
- brain-first-generate got further this time: 4 batches, 18 moments extracted, 12 deduped → 6 unique
- SIGKILL'd during "Generating posts from 6 unique moments" — the post-generation LLM call
- Consistent pattern: extraction succeeds, post generation OOMs
- 3 kills tonight (2 from earlier + this one). Deferring to morning cron.
- **Root cause hypothesis:** The post-generation step likely loads all 6 moments + system prompt + few-shot examples into one LLM call, spiking memory. Reducing to 1-2 posts per call or streaming would fix it.

## Nightly Value Build (1:00 AM)

### Artist Intelligence Demo Page
- Built interactive demo at `/learn/demos/artist-intelligence` on marketing site
- API route proxies to Recoup Research API with web intelligence fallback
- Tested locally — web fallback works, returns rich streaming data from ChartMasters etc.
- Branch: `feature/artist-intelligence-demo` pushed to `recoupable/marketing`
- Vercel auto-deploys preview from branch

### Chartmetric API 401 Bug
- ALL Chartmetric-backed research endpoints returning 401 errors
- Affects: `/research/profile`, `/cities`, `/similar`, `/insights`, `/metrics`, etc.
- Web intelligence (POST) endpoints work fine — different backend
- This affects ALL API users, not just the demo. Sid/Sweets need to rotate the CM token.
- Demo built with automatic fallback — upgrades to structured data when CM is fixed

### Category Rotation
- Last 4 nights (May 27-29): content pipeline + sales collateral
- Tonight: marketing site product experience (different category)
- Content pipeline stable: 6 ready drafts with thumbnails in queue

## #cosmo-review Channel Cleanup (12:10 PM)
- Sid asked to purge all messages from #cosmo-review and keep it clean going forward
- Deleted 8 bot messages; 4 couldn't be deleted (Sid's own messages + channel join system msgs — Slack API limitation)
- Updated `slack-review.js` with `deleteReviewMessage()` helper — now auto-deletes review messages from Slack after:
  - 👍 approval (reaction or text-based)
  - 👎 rejection
  - 💬 feedback (deletes old message before revision; revised version re-posts fresh)
- Also deletes thread replies (thumbnails, etc.) before the parent message
- Clears `slack_message_ts`/`slack_channel` in DB after deletion so draft isn't tracked as pending
- Result: #cosmo-review will always have at most 1 pending post visible

## Content Engine — 4th OOM Kill + Watcher Paused (11:49 PM)
- brain-first-generate SIGKILL'd a 4th time tonight — same OOM during post generation
- Root cause of repeated kills: TWO duplicate cron watchers running every 5 min:
  - `slack-review-watcher` (7c1546ca) — cron */5
  - `slack-review-watch` (f7a3b1c2) — every 5m
  - Both trigger `slack-review.js watch`, both see empty queue, both fire mini-pipeline, both OOM
- **Action:** Disabled BOTH watchers for the night to stop the kill loop
- **TODO for morning:**
  1. Remove the duplicate watcher (keep one, delete other)
  2. Fix brain-first-generate OOM — reduce batch size or split post generation into individual calls
  3. Re-enable single watcher after fix
- Morning cron (7:45 AM) will attempt to seed the queue; may also OOM without the fix

## Slack Review Bug Fixes (1:00 PM - 10:30 PM)

### Timestamp Tracking Bug (Fixed)
- `files.completeUploadExternal` doesn't return message ts directly
- Old approach: poll `conversations.history limit=1` — grabbed stale/wrong messages
- Fix: record `beforePostTs` before posting, only accept messages newer than that in the poll
- Also try extracting ts from the `completeUploadExternal` response (`files[].shares.public.<channel>[].ts`)

### Cron Watcher
- Set up `slack-review-watcher` cron every 5 minutes (job ID: 7c1546ca)
- Runs `slack-review.js watch` in isolated session, no delivery (silent)

### Thread-Based Feedback Flow (10:30 PM)
- Sid's request: when giving feedback via thread, the revised version should post IN the thread, not as new top-level message
- Changes to `slack-review.js`:
  - `postDraftToSlack()` now accepts `{ threadTs }` option — posts as thread reply when set
  - Feedback detection: keeps original message (no delete), saves parent ts in `revisionThreadMap`
  - After regeneration, revised draft posts as reply in the original thread
  - Reactions on thread replies: detects `thread_ts !== ts` to identify revisions
  - Cleanup: `deleteReviewMessage()` accepts `{ alsoDeleteParent }` — deletes both the reply and the parent thread
  - Thread feedback: checks parent thread for new feedback posted AFTER the latest bot revision
  - Multi-round revisions: uses parent ts (not reply ts) for subsequent revisions in same thread
