# 2025-05-16

## Content Engine Fixes (Sid's Request)

### Problem
1. 38 of 51 approved posts had no thumbnails (approved before auto-slides ran)
2. Zero X (Twitter) drafts — nightly engine hardcoded `linkedin` as target_platform

### Fixes Applied

**Thumbnail backfill:**
- Created `scripts/batch-thumbnails-approved.js` — generates gpt-image-2 thumbnails for approved drafts with null media_urls
- Running in background, ~2 min per image × 38 = ~76 min total

**X pipeline fix (nightly-engine.js):**
- Removed `target_platform = 'linkedin'` hardcoding from `autoPov()`
- POV update now only sets `pov` without forcing a platform

**X pipeline fix (generate.js):**
- `assignPlatforms()` now returns two entries per idea: linkedin + x (was returning only one)
- Added duplicate check: skips if draft already exists for idea+platform combo
- Explicit platform selection in dashboard still respected (x-only, youtube-only)

**X draft backfill:**
- Created `scripts/backfill-x-drafts.js` — converts approved LinkedIn posts to X threads/tweets
- Ran successfully: 48/48 X drafts created, saved as 'ready' status in Supabase
- These go through normal review/approve flow in the dashboard

### Files Changed
- `~/Documents/projects/content-engine/scripts/nightly-engine.js` — removed linkedin hardcoding from autoPov
- `~/Documents/projects/content-engine/scripts/generate.js` — multi-platform generation + duplicate check
- `~/Documents/projects/content-engine/scripts/batch-thumbnails-approved.js` — new (one-off)
- `~/Documents/projects/content-engine/scripts/backfill-x-drafts.js` — new (one-off)

### Note
`schedule-approved.js` already supports both X and LinkedIn via Postiz integrations. No changes needed there.
