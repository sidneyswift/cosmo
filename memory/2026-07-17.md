# 2026-07-17

## Channel Rules
- **#chat** (C0AK88J4SPK): Sid asked me to turn OFF auto-respond. Only reply when directly @mentioned. Do NOT respond to general chatter.

## Homa
- Sid confirmed +homatest sandbox families are already fenced from physical mail (thank-you cards). Verified: `isSandboxSignupEmail()` → `sandbox=true` → `and not f.sandbox` in thank-you card sweep. No code change needed.
- Explained the inspo funnel to Sid: #homa-inspo drop → breakdown in-thread → inspo-catalog.md → homa-taste.md → materials-studio SKILL.md → reference images → agent uses recipes. Gap identified: taste model → SKILL.md promotion is manual/latent.
- **Founder ruling: #homa-feedback deploy-first-reply-second.** Feedback → implement → branch → PR → CI green → merge → deploy to prod → THEN reply. Users are non-technical, expect the real app to update. Only ask clarifying questions if feedback is vague or large. Updated TOOLS.md.
- **Hero doodle overlap fix (round 2).** First fix only repositioned doodles with new `top`/`left` values but didn't account for mobile breakpoint — stickers still overlapped on phones. Sid caught it: "you didn't update mobile." Second fix: wrapped star+sun doodles in `.lp-hero-doodles` div, added `display: none !important` in the `@media (max-width: 880px)` block. Lesson learned the hard way.
- **Created `frontend-feedback` skill.** Sid's directive: "ALWAYS UPDATE DESKTOP AND MOBILE. ALWAYS CHECK HOW YOUR WORK LOOKS AND REASON/ITERATE BEFORE MERGING." Skill codifies: fix all viewports, verify visually before merge, deploy-then-confirm, never guess pixel values.
